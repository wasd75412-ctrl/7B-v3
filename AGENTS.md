# 7B 專案共用開發規則

本檔適用於整個儲存庫及所有分支。任何功能分支都必須從最新的 `main` 建立，合併前重新同步 `main`，不得在分支內另訂互相衝突的流程或產品語意。

## 完成與提交

- 每一批完成的程式變更都必須依序執行：更新版本、執行 `npm test`、執行 `npm run build`、提交 Git commit。
- 除非使用者明確要求暫不提交，完成且驗證通過的工作不可只留在工作目錄。
- 不得把分析輸出、預覽日誌、臨時截圖或其他除錯產物加入提交。
- 一個分支只處理其命名所描述的工作；發現其他未提交變更時必須保留並排除，不可擅自混入。

## 完成即發布

- 本規則適用於目前所有分支及日後新增的每一個分支，不因工作樹、工具、工作階段或分支名稱而例外。
- 「完成」的定義不是只完成程式或提交，而是依序完成：升版、`npm test`、`npm run build`、commit、push、建立 pull request、等候共用 CI、合併至 `main`、確認 `main` CI、確認 Netlify 正式站已載入新版本與新雜湊資產。
- 除非使用者在該次工作明確要求暫停、暫不發布或保留分支，代理程式不得在 commit、push、pull request 或 Deploy Preview 階段停下等待下一道指令；必須自行持續到正式站驗證完成。
- CI 失敗時不得合併；應修正後重新執行完整流程。外部服務故障或權限不足時，必須先用安全範圍內的方法排除，確實無法繼續才回報阻塞原因。
- 合併完成後必須刪除遠端功能分支並清理本次建立的工作樹，不得把已發布工作留成待處理分支。

## BCM 版本規則

- `package.json`、`package-lock.json` 與 `package-lock.json` 根套件的版本必須完全一致。
- 一般修正或小幅功能更新時，patch 加一。
- patch 從 49 再進位時歸零，minor 加一，例如 `2.4.49` 下一版為 `2.5.0`。
- minor 從 9 再進位時歸零，major 加一，例如 `2.9.49` 下一版為 `3.0.0`。
- 不得讓 patch 大於 49，也不得讓 minor 大於 9。

## 測試模式的固定語意

- 測試模式必須能走完正常比賽的全部操作，包括計分、結束比賽、結果畫面、下一場輪轉與復原。
- 結束測試比賽時不得跳離、失效或阻止正常流程。
- 測試比賽不得寫入或保留在比賽紀錄中；既有的測試紀錄也必須在載入時排除。
- 測試比賽不得計入任何戰績、勝率、場次、連勝、搭檔統計、排行榜、儀表板統計或球員生涯資料。

## 分支合併門檻

- 所有分支的 push 與 pull request 都必須通過共用 CI。
- CI 失敗的分支不可合併；不可只在自己的分支停用或繞過檢查。
- 共用規則若需調整，必須在 `main` 統一修改本檔與 CI，再由其他分支同步。
